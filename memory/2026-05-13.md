# memory/2026-05-13.md

## Features Shipped (playtest-round-3 batch)
- **Bet vs Raise labels** (PokerTable + Raise modal). Preflop = Raise. Postflop with no bet = Bet. Postflop after a bet = Raise. Server unchanged (still accepts `raise` for both); label-only fix.
- **Phantom-players fix**: backend now also pushes a full `game:state` to every seated player on join (not just the `player:joined` notification). Frontend GameRoom additionally does a `loadGameState()` + delayed `loadGameState()` 500ms later for replication-lag protection. GameRoom switched from raw `socket.emit('join:game')` to the `useSocket().joinGame()` helper so reconnects auto-rejoin the room.
- **End-of-hand modal on heads-up game-end**: when game.status flipped to `completed` the GameRoom component returned early into the Game Over screen, bypassing the fold-win modal. Now gated `&& !foldWinData && !showdownData` so the hand result shows first, dismisses, then Game Over.
- **Turn timer reduced 30s → 17s**, warning 10s → 7s. `TURN_TIMEOUT_MS=17000`, `TURN_WARNING_MS=7000`.
- **Urgent turn alert sound** (synthesized 3-tone, harsher square-wave) fires once when entering the 7s alert window. New `playUrgentTurnAlert()` in `utils/sounds.ts`.
- **Bot thinkMs default 1500 → 0**. Shaun reported the 1.5s caused inconsistent UX (sometimes instant, sometimes 4-5s pause then a burst of bot actions). Removed entirely.
- **Settings: audio + popup toggles**. Extended `audioPreferences.ts` with `popups` (in-table hand-result modals) and `urgentAlert` (the 7s ping). AudioToggle settings panel now shows 4 toggles. Compact button still cycles sound/notify.
- **Card-deal animation** (`DealAnimation.tsx`): on every `game:new-hand`, flicks face-down cards from dealer button to each seated player clockwise from SB, 2 passes, soft synthesized deal sound per card. Honours `getAudioPrefs().sound`. SEAT_POSITIONS and getRelativeSeatPositions exported from PokerTable.

## Decisions
- All hand-result popups gated behind new `audioPrefs.popups`. Default true.
- Card-deal animation is pure overlay; doesn't change PokerTable internals.
- Urgent alert + deal sounds are synthesized (Web Audio API) instead of audio assets. Zero asset shipping, easy to swap later.
- Mobile-responsive layout (#6) deferred to next session. Audit plan at `audits/t3-poker/15-responsive-layout-plan.md`.

## Changes Approved By Shaun
- All 8 items from 2026-05-13 morning brief; #6 deferred at his request.

## Bugs Resolved
- Match-creator phantom players bug (had to refresh).
- Fold-win + heads-up game-end → no modal.
- Bot action burst pattern (thinkMs 1500ms).

## Follow-Ups
- **Next session**: implement #6 from the responsive-layout-plan audit.
- Watch for: backend per-action latency (still ~350ms TIMING but Shaun reported felt delay between humans — investigate if persists after this push).
- Verify in next playtest: timer sync visually matches server (with `Hand.turnStartedAt` driving both, drift should be zero).

## Promote To MEMORY.md
- TURN_TIMEOUT_MS = 17s (was 30s)
- TURN_WARNING_MS = 7s (was 10s)
- bot thinkMs default = 0 (was 1500)
- `audioPreferences` keys: `poker.audio.sound`, `poker.audio.notify`, `poker.ui.popups`, `poker.audio.urgent`
- DealAnimation overlay component pattern (mounts on game:new-hand, uses SEAT_POSITIONS + getRelativeSeatPositions from PokerTable)
- Fold-win modal must be checked BEFORE the gameState.status==='completed' early-return in GameRoom
